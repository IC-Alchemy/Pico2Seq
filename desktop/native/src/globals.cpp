// Desktop definitions of extern globals that the firmware defines in files
// not compiled into the DLL (currently just the legacy slide flag that
// Voice.cpp reads; AppState.cpp owns everything else it declares).

bool slideMode = false;
